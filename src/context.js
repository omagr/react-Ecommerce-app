import React from 'react'
/*
1. createContext is a mehtod in React
2. we have to choose a page where every component exist that we have need to pass DataTransfer
*/
export const CartContext = React.createContext()
export const SearchContext = React.createContext()